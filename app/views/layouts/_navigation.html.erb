<nav class="navbar navbar-expand-lg navbar-light bg-light" style="background-color: #c5daea !important;">
  <div class="container">
    Real Estate
    <button
      class="navbar-toggler"
      type="button"
      data-mdb-toggle="collapse"
      data-mdb-target="#navbarButtonsExample"
      aria-controls="navbarButtonsExample"
      aria-expanded="false"
      aria-label="Toggle navigation"
    >
      <i class="fas fa-bars"></i>
    </button>
    <div class="collapse navbar-collapse" id="navbarButtonsExample">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <a class="nav-link" href="/">Dashboard</a>
        </li>
      </ul>
      <% if user_signed_in? %>
        <div class="d-flex align-items-center">
          <%= link_to '<button type="button" class="btn btn-link px-3 me-2">Edit profile</button>'.html_safe, edit_user_registration_path %>
          <%= link_to '<button type="button" class="btn btn-primary me-3">Logout </button>'.html_safe, destroy_user_session_path, method: :delete %>
      <% else %>
        <div class="d-flex align-items-center">
          <%= link_to '<button type="button" class="btn btn-link px-3 me-2"> Login </button>'.html_safe, new_user_session_path  %>
          <%= link_to "" '<button type="button" class="btn btn-primary me-3">Sign up for free</button>'.html_safe, new_user_registration_path  %>
        </div>
      <% end %>
    </div>
  </div>
</nav>
